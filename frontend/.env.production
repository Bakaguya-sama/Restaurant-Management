# Frontend API Configuration - Production
# When deployed on Vercel, the frontend will automatically detect the backend URL
# from the current window location and use port 5000 for the API

# For explicit backend URL in production (optional)
# If the backend is at a different domain, set this explicitly
# Otherwise, apiConfig.ts will detect from window.location

# This will be automatically set by apiConfig.ts:
# If VITE_API_BASE_URL is not set, it detects:
# - protocol from window.location.protocol
# - hostname from window.location.hostname  
# - Uses port 5000 if running on dev ports (5173 or 3000)
# VITE_API_BASE_URL will be set by vercel environment variables if needed

# Uncomment and set if backend is on a different domain/port:
# VITE_API_BASE_URL=https://backend.example.com/api/v1

# Base URL for image serving
# VITE_BASE_URL=https://backend.example.com
