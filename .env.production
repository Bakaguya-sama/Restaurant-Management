# Production Environment Variables
# These should be set in Vercel dashboard for actual deployment

# MongoDB Atlas connection string (must be set in Vercel dashboard)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/restaurant_management

# Backend URL - set by Vercel after deployment
# This will be used by the frontend to communicate with the backend
BACKEND_URL=https://your-backend.vercel.app

# Node environment
NODE_ENV=production

# Server port
PORT=3000
